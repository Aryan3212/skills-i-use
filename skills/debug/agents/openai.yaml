interface:
  display_name: "Debug"
  short_description: "Find a root cause with evidence"
  default_prompt: "Use $debug to reproduce and isolate this failure; fix it only if requested."
policy:
  allow_implicit_invocation: true
