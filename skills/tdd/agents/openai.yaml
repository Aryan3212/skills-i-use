interface:
  display_name: "TDD"
  short_description: "Build behavior through red-green cycles"
  default_prompt: "Use $tdd to implement this behavior test-first at the right public seam."
policy:
  allow_implicit_invocation: false
