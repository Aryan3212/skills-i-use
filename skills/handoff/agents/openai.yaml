interface:
  display_name: "Handoff"
  short_description: "Prepare a concise continuation brief"
  default_prompt: "Use $handoff to prepare this work for a fresh agent or human."
policy:
  allow_implicit_invocation: true
