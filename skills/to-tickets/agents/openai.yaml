interface:
  display_name: "To Tickets"
  short_description: "Split a spec into verifiable tasks"
  default_prompt: "Use $to-tickets to break this approved feature document into implementation tasks."
policy:
  allow_implicit_invocation: true
